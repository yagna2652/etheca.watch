{
  "buildCommand": "cd react-website && npm run build",
  "outputDirectory": "react-website/build",
  "installCommand": "cd react-website && npm install",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}